<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cos.shop.repository.HateRepository">

 <insert id="hate">
  	insert into hate(userId, productId)
  	values(#{userId}, #{productId})
  </insert>
  
  <delete id="hateMinus">
  	delete from hate where userId = #{userId} and productId = #{productId}
  </delete>
  
  <select id="findByUserIdProductId" resultType="int">
  	select count(*) from hate where userId = #{userId} and productId = #{productId}
  </select>
  
</mapper>