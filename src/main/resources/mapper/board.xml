<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cos.shop.repository.BoardRepository">

	<insert id="boardWriteProc">
		insert into board (title,content,userId,productId) values(#{title},#{content},#{userId},#{productId})
	</insert>
	
	<select id="findAllProductId" resultType="com.cos.shop.model.board.dto.RespBoardDto">
		select b.id,b.title,b.content, b.productId,b.createDate,u.username
		from board b
		inner join user u
		on b.userId = u.id
		where productId = #{productId}	
		order by id desc
	</select>
	
	<select id="findById" resultType="com.cos.shop.model.board.dto.RespBoardDto">
		select b.id,b.title,b.content,b.productId, b.createDate,u.username
		from board b
		inner join user u
		on b.userId = u.id
		where b.id = #{boardId}	
	</select>
	
	<update id="updateProc">
		update board set title = #{title}, content =#{content}
		where id = #{id}			
	</update>
  
  	<delete id="delete">
  		delete from board where id =#{boardId}
  	</delete>
  
</mapper>